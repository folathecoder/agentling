"""Runnable agentling examples.

This package marker lets the tests import the example modules (e.g.
`import examples.cli_failure_recovery`). Each example is still self-contained and
runnable directly, for example `uv run python examples/cli_failure_recovery.py`.
"""
